<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:name=".ZeinFlowApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ZeinFlow"
        tools:targetApi="31">
        
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.ZeinFlow">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity
            android:name=".ui.auth.GoogleSignInActivity"
            android:exported="false"
            android:theme="@style/Theme.ZeinFlow" />
            
        <activity
            android:name=".ui.transaction.AddTransactionActivity"
            android:exported="false"
            android:theme="@style/Theme.ZeinFlow" />
            
        <activity
            android:name=".ui.category.CategoryManagementActivity"
            android:exported="false"
            android:theme="@style/Theme.ZeinFlow" />
            
        <activity
            android:name=".ui.report.ReportActivity"
            android:exported="false"
            android:theme="@style/Theme.ZeinFlow" />
    </application>

</manifest>